<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="XamApp.Views.CardsView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:res="clr-namespace:XamApp.Resources.Strings"
    Title="{x:Static res:Strings.MyCards}">
    <ListView ItemsSource="{Binding Cards}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Label Text="{Binding .}" />
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>